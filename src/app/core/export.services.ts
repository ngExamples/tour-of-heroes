import { HeroService } from './services/hero.service'
export { HeroService } from './services/hero.service'

/**
 * contains all services in core module
 */
export const SERVICES = [
    HeroService
];
